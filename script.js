// Basic JavaScript: Access Array Data with Indexes
// Access the data inside arrays using indexes
// Array indexes are written in the same bracket notation that strings use
// Except that instead of specifying a character, they are specifying an entry in the array
// Use zero-based indexing, so the first element in an array has an index of 0

var myArray = [50,60,70];

var myData = myArray[0];

console.log(myArray[0]);
